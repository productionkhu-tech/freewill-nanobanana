# NanoBanana 인수인계 — 2026-07-20 전체판

이 문서 하나로 "앱이 뭔지, 어떻게 생겼는지, 오늘까지 무슨 일이 있었는지, 다음에 뭘 하면 되는지"를 파악할 수 있게 쓴 종합 인수인계서입니다.
상세 규칙은 [CLAUDE.md](CLAUDE.md), Reve 실측 일지는 [인수인계_2026-07-15_Reve2.1.md](인수인계_2026-07-15_Reve2.1.md), 배포 절차는 `.claude/skills/나노바나나api배포/SKILL.md` 참고.

---

## 1. 제품 개요

- **무엇**: 멀티 프로바이더 AI 이미지 생성 Windows 데스크톱 앱 (재판매 상품 — 고객에게 EXE + 키 설치 bat 전달)
- **스택**: Flask(127.0.0.1:5656, app.py 단일 파일) + pywebview(WebView2 창) + PyInstaller onefile EXE (~35MB)
- **배포**: GitHub Releases — 앱이 부팅 시 자동으로 새 버전 감지 → 팝업 → 원클릭 자기 교체
- **현재 배포 버전**: **v2026-07-2004** (2026-07-20에 2001→2002→2003→2004 연속 릴리스)
- **저장소**: https://github.com/productionkhu-tech/freewill-nanobanana (main 단일 브랜치)

## 2. 폴더/파일 지도

### 프로젝트 루트 = 인수인계서 저장 폴더
```
C:\Users\user\Desktop\기획 파일\TA\앱개발\나노바나나 api\
├── app.py                  # Flask 서버 + AppState(모든 상태/로직), ~4000줄
├── launcher.py             # pywebview 창 수명주기 + 단일인스턴스 + --updater 모드 + 자동업데이트 체크
├── updater.py              # 버전 체크(API→raw 폴백) + 403 조립URL/사이드카 폴백 + EXE 자기교체
├── VERSION                 # 현재 버전 (커밋 대상)
├── static/app.js           # 메인 프론트 (~1900줄)  static/style.css
├── templates/index.html    # 메인 UI
├── templates/viewer.html   # 프리뷰 창 (자체 완결 — 뷰어 로직 전부 이 파일 안)
├── templates/prompt_popup.html
├── dist/NanoBanana/        # 배포 산출물 (NanoBanana.exe + setup_env.bat)
├── _retired/               # 은퇴한 Reve 레이아웃 에디터 소스 (빌드 제외, 기록 보존)
├── .claude/skills/나노바나나api배포/SKILL.md   # 배포 절차 스킬
├── CLAUDE.md               # 개발 규칙/원리/함정 (필독)
├── 인수인계_*.md            # 인수인계 문서들 (이 폴더에 계속 쌓음)
└── Reve API 문서.md         # Reve REST 스펙 전문 + 실측 주석
```

### 빌드 보조 (한글 경로 회피용)
```
C:\NanoBanana_build\
├── src\      # 빌드 시 소스를 여기로 복사 (PyInstaller가 읽음)
└── venv\     # 빌드 전용 파이썬 (⚠ CLAUDE.md의 venv_build 표기는 낡음 — venv가 맞음)
```

### 키 파일 (전부 git 금지, .gitignore 등록됨)
- `레베 api key.bat` (루트) — Reve 키 설치기. 사용자 환경변수 REVE_API_KEY로 설치(오늘 이 PC에 설치 완료)
- `F:\나노바나나\reve api key.bat` — 고객 배포용 사본 (오늘 생성)
- `setup_env.bat` — Vertex 등 기존 키 설치기 (배포 시 동봉)

### 사용자 데이터
- `~/.nanobanana/` — prefs, last_seen_version, reve_last_debug.json(레베 디버그 덤프)
- `~/Documents/NanoBanana JSON/` — 프로젝트 저장
- `~/Desktop/NanoBanana_Output/` — 기본 출력 폴더

## 3. 모델/프로바이더 현황

| 프로바이더 | 모델 | 비고 |
|---|---|---|
| Google Vertex/Studio | gemini-3-pro-image 등 | 10RPM → 앱이 8RPM으로 스로틀 |
| OpenAI | gpt-image-2 | 4K, 생성 ~2분 |
| BytePlus | Seedream 5.0 계열 | |
| Reve | reve-create (생성 전용) | **에디터는 제거됨**. 누끼 체크박스 = 생성 결과물에 remove_background 후처리(입력 이미지들을 각각 따주는 게 아님). 예산은 계정 단위 — 402면 콘솔 충전 |

## 4. 이 세션(2026-07-17~20)에서 한 일

### A. Reve 레이아웃 에디터 → 최종 제거
부분 편집을 문서 파이프라인대로 붙이며 실측(무시되는 change/shift, bbox 강제, 앵커 규칙, 크롭 폴백 등 — 상세는 Reve2.1 일지 §11~12)까지 갔으나, 사용자 결정("생성만 냅둬")으로 **에디터 UI/라우트 전체 제거**. 소스는 `_retired/`에 보존. 측정 지식은 문서로 남음(부분편집을 되살릴 때 그대로 재사용 가능).

### B. 프리뷰(뷰어) 전면 개편 — viewer.html
- **화질**: 캔버스 DPR 보정(윈도우 배율에서 소프트해지던 것), 갤러리 1칸=원본 서빙, 썸네일 q92
- **조작**: 휠=이전/다음(140ms 게이트), Ctrl+휠=줌, 드래그=팬, 더블클릭/0=화면 맞춤, 좌우 16% 클릭존(줌 중에도 동작), Esc, F=OK컷, S=선택, Ctrl+C/우클릭=클립보드 복사, 첫 실행 5초 단축키 안내
- **창 관리**: 단일 인스턴스(열려 있으면 내용만 교체), 최대화 상태 유지(restore는 최소화일 때만), 새 창은 메인 창 위 90% 중앙(Win32 물리좌표 — 멀티모니터/DPI 안전), 창 크기 기억은 폐기(사용자 방향)
- **갤러리 연동**: 보는 이미지를 메인 갤러리가 실시간 추적(금색 테두리+"미리보기" 배지, 닫아도 유지), 선택/OK컷은 메인에 미러링, 폴더 버튼
- **스플릿(와이프) 비교**: 갤러리 2장 선택→"⇆ 비교" 버튼 or 카드/외부파일 드래그앤드롭. 겹쳐놓고 가운데 선 드래그(좌=기준/우=비교), 배율은 고해상도 쪽 기준+양쪽 동일 표시높이, 줌/팬 동기화, 코너 패널(파일명·모델·해상도 + ★OK컷/폴더 버튼), 휠=커서 쪽 이미지 전환, 드롭=놓는 쪽 배치(50:50 판정 — 디바이더와 무관), 이미지 바뀌면 디바이더 중앙 리셋, 좌우 정보로 "뭐가 더 좋은지" 마킹(OK컷=★즐겨찾기 단일 개념)
- **안정화**: 로드 레이스 가드(빠른 휠에도 화면=경로=버튼 일치), gallery_rev 1.2s 폴링으로 삭제(자동 이동/비교 닫힘)·신규 생성·★변경 실시간 반영, 카드 드래그는 서버 릴레이(같은 프로세스 WebView2 OLE 드롭 데드락 회피 — 외부 파일만 네이티브 드롭)

### C. 갤러리/기타
- Explorer 버튼 첫 클릭에 파일 선택(SHOpenFolderAndSelectItems — 기존 2회 클릭 버그 수정)
- 에디터 진입 버튼류 제거, ⇆ 비교 버튼 추가

### D. 배포 사가 (오늘의 하이라이트)
1. **v2026-07-2001**: Reve 생성 + 뷰어 개편 릴리스
2. **"업데이트 안 뜬다" 제보 → 중대 발견**: `launcher.py`의 자동 업데이트 체크 스레드 기동이 **v2026-06-1201부터 주석 처리**된 채 배포되고 있었음("NanoGPT 프로토타입" 흔적). 6/12~7/20-01 사용자 전원 부팅 팝업 없음. (수동 푸터 클릭은 전 버전 동작)
3. **v2026-07-2002**: 스레드 복구 핫픽스. 부팅 3초 내 이벤트 발신 실측 검증
4. **v2026-07-2003**: 검증 릴리스 — 2002가 켜지자마자 자동 팝업 뜨는 것 사용자 눈으로 확인
5. **403 제보 → 대응**: GitHub 무인증 API 제한(IP당 60회/h, 공유망) 진단. 업데이터에 **조립 URL + sha256 사이드카 폴백** 구현(설치 단계가 API 없이도 동작)
6. **v2026-07-2004**: 폴백 탑재 릴리스. 이후 **릴리스마다 EXE + NanoBanana.exe.sha256 두 자산 업로드 필수** (2003에도 소급 업로드)
- 매 릴리스: 실제 구버전 바이너리로 감지→설치→교체 E2E PASS

### E. 환경/도구
- REVE_API_KEY를 사용자 환경변수로 설치(그동안 미설치 상태였음 — 실행 시마다 수동 주입해왔던 것)
- 배포 스킬 생성: `.claude/skills/나노바나나api배포/` — 다음 세션에서 "배포해줘"만 하면 절차 자동 적용

## 5. 아키텍처 핵심 (코드 잡을 때)

- **app.py / AppState**: 설정·갤러리·ref·큐·잠금 전부 이 싱글톤. 공유 상태는 반드시 락(gallery_lock 등). 라우트 ~45개, POST는 X-NB-Token CSRF 필수(GET 제외). `gallery_rev` = 갤러리 변경 카운터(뷰어 동기화용 — add/delete/favorite에서 증가)
- **launcher.py**: 단일 인스턴스(mutex+포트 폴백), `--updater` 서브커맨드(스왑 전용), JsApi(open_viewer 등), `_bg_update_check`(**절대 다시 끄지 말 것** — §4-D 참조), `_place_viewer_over_main`(뷰어 창 물리좌표 배치)
- **updater.py**: 체크=Releases API→raw 폴백 / 설치=자산 API→**403 시 조립 URL+사이드카 폴백** / 다운로드 검증=sha256 / 스왑=새 EXE를 `--updater`로 스폰(write-probe → os.replace)
- **viewer.html**: 서버 의존 최소(이미지·갤러리·rev·릴레이 API만). 비교/줌/드롭/동기화 로직 전부 이 파일
- **이벤트 흐름**: 서버 push_event → 메인 창이 /api/events 폴링(다른 데서 폴링하면 이벤트 뺏김 주의!). 뷰어→메인 통신도 이 큐(viewer_state/select/refresh). 카드→뷰어 드래그는 /api/viewer/drag 릴레이

## 6. 배포 방법

`.claude/skills/나노바나나api배포/SKILL.md`에 전 절차 + 스크립트 전문. 요약:
VERSION bump(같은 날 NN 증가) → 클린 빌드+sha256 → 시크릿 스캔+이름 지정 커밋+태그 → urllib로 릴리스 생성(한국어 body + `sha256:` 줄) → **EXE와 사이드카 2종 업로드** → 구버전 바이너리 E2E → 로컬 재기동 → 인수인계 기록.

## 7. 사용자 안내 필요 사항 (미결 포함)

| 대상 | 안내 |
|---|---|
| **v2026-06-1201 ~ 07-2001 사용자** | 부팅 팝업이 없는 빌드 → **앱 하단 버전 숫자 클릭 → 지금 설치** 1회 필요. 이후 자동 복귀 |
| 업데이트 중 HTTP 403 | 공유망 제한 → 1시간 후 재시도 / 다른 네트워크 / 릴리스 페이지 수동 다운로드. 2004+부터는 자동 우회 |
| Program Files 설치 | 자동 업데이트 불가(UAC) — 일반 폴더로 이동 |
| Reve 402 | 키 교체로 해결 안 됨(계정 예산) — https://api.reve.com/console 충전 |

## 8. 다음 할 일 후보

1. 구버전 사용자 공지 발송(§7 첫 줄) — 아직 안 나감
2. 뷰어 1칸 성능: 원본 서빙이 저사양에서 무거우면 2048px 고품질 썸네일 옵션으로 다운시프트
3. upscale 토글(미구현), Reve 부분편집 부활 여부(측정 지식은 Reve2.1 일지에 온존)
4. 실기기 확인 잔여: 크로스윈도우 카드 드래그(릴레이) 실마우스 체감, 멀티모니터 뷰어 배치 재확인

## 9. 문서 지도

| 문서 | 내용 |
|---|---|
| **이 문서** | 종합 인수인계 (여기서 시작) |
| CLAUDE.md | 개발 규칙·빌드/배포 명령·아키텍처 결정사·함정표 (필독) |
| 인수인계_2026-07-15_Reve2.1.md | Reve 통합~에디터 은퇴까지 실측 일지 + 오늘 세션 상세 로그(§11~18) |
| .claude/skills/나노바나나api배포/SKILL.md | 배포 절차 스킬 |
| Reve API 문서.md | Reve REST 스펙 + 실측 주석 |
| ~/.claude/.../memory/ | 세션 간 기억(배포 상태, 빌드 경로, PS 함정) |


---

## 릴리스 추가 기록 (실제 작업일: 2026-07-30)

### v2026-07-2101 — 뷰어 단일 프리뷰 비율 왜곡 수정 (E2E PASS)
- 증상: 단일 이미지 프리뷰가 실제 비율보다 퍼져 보이고, 더블클릭하면 정상 복귀 (사용자 제보)
- 원인: 프롬프트 푸터(48~160px 가변)가 비동기 /api/gallery 응답으로 자라며 스테이지 높이를 뺏는데,
  창 resize 이벤트가 없어 fit()이 잡아둔 캔버스 백킹스토어가 줄어든 CSS 박스에 눌려 비율 왜곡.
  비교 모드는 _applyGalleryData의 render()로 자가 치유돼 단일 뷰만 발병 (간헐 = 이미지 디코드 vs 갤러리 fetch 완료 순서 레이스)
- 수정: viewer.html #stage에 ResizeObserver 추가 — 푸터 성장/창 리사이즈/최초 SetWindowPos 배치 등
  모든 박스 변화에 즉시 재fit. 기존 debounce resize 리스너는 안전망으로 유지
- E2E: v2026-07-2004 실바이너리 부팅 → 감지 → 설치 → 스왑 → v2026-07-2101 부팅 + sha256 일치 PASS.
  부팅 자동체크 "Already on latest version" 로그 확인. 자산 2종(EXE+사이드카) 업로드 완료
- 함께 커밋: .gitignore에 맥 키 설치 문구 파일(맥_키_설치_붙여넣기.txt) 보호 항목

### 같은 날 맥 지원 작업 (릴리스 외, 로컬)
- 맥 고객 키 원클릭 설치 문구 생성: 맥_키_설치_붙여넣기.txt (로컬 전용, gitignore 등록).
  터미널에 붙여넣으면 ~/.nanobanana/에 keys.env + service_account.json 설치. 5개 프로바이더 전부 포함.
  경로 무관(홈 절대경로), LF 줄바꿈으로 저장(CRLF면 heredoc 종결자 깨짐)
- 맥 Gemini 전멸 진단: Apple CLT Python 3.9에서는 google-genai가 1.47.0까지만 설치되는데
  1.47.0의 ImageConfig에는 image_size 필드가 없음(aspect_ratio뿐) → 2K/4K 요청이 pydantic
  extra_forbidden으로 즉사(0.0s). 윈도우 빌드 venv는 google-genai 2.8.0이라 정상.
  해결 = python.org Python 3.10+(3.12 권장) 설치 + Install Certificates.command 실행 + 의존성 재설치
- 보류 중(미적용): server_mac.py 파이썬 3.10 미만 가드, requirements_mac.txt google-genai>=2.8 핀,
  맥 가이드 python.org 필수 강조
### 추가 (2026-07-30) — 맥 SSL 사건 + 재발방지 3종 적용 완료 (커밋 5f3e086, push됨)
- 고객 맥에서 Reve만 CERTIFICATE_VERIFY_FAILED: python.org 파이썬의 Install Certificates.command
  미실행이 원인. SDK 프로바이더(httpx+certifi 내장)는 멀쩡, 표준 urllib 쓰는 Reve/웹드래그/릴리스노트만 사망
- server_mac.py: SSL_CERT_FILE을 certifi 번들로 자동 지정(_fix_ssl_certs) + Python 3.10 미만 즉시 종료 가드
- requirements_mac.txt: google-genai>=2.8 핀 + certifi 명시 / 가이드: python.org 필수 + 인증서 단계 + 문제해결 표
- 주의: server_mac.py 자체는 엔트리 스크립트라 git pull 받은 "다음" 실행부터 새 코드 적용
  (당장은 고객 맥에서 Install Certificates.command 더블클릭이 즉효)
- 뷰어 사소 후보 2건(비교 오른쪽 onerror 부재, 자기비교 허용)은 여전히 미적용 — 다음 릴리스 후보
### v2026-07-2301 (실제 작업일 2026-07-30) — Seedream 5.0 Pro 커스텀 최대픽셀 상한 교정 (E2E PASS)
- 증상: Seedream 5.0 Pro + Custom에서 4800x960 같은 통과 가능한 크기가 "최대픽셀 축소"로 4576x912로 잘림 (사용자 제보)
- 원인: _SEEDREAM_CUSTOM pro 상한을 2048^2(4,194,304)로 박아둠. 실제 BytePlus Method-2 한도는
  문서상 2048*2048*1.1025 = 4,624,220. GPT(8,294,400)도 아니고 Seedream 실값도 아닌 어정쩡한
  근사치라 ~10.25% 짜게 잘림. 4800x960 = 4,608,000px(5:1, 비율한도 16 이내)는 원래 통과해야 함
- 수정: app.py _SEEDREAM_CUSTOM + static/app.js _SEEDREAM_CUSTOM_JS 미러 둘 다 4_624_220으로.
  검증: 4800x960 무변형 통과 / 2048x2048 무회귀 / 5000x1000은 여전히 4816x944로 정상 클램프.
  seedream-4-5(4-5 문서 없어 미변경), gpt-image-2 미변경
- E2E: v2026-07-2101 실바이너리 -> 감지 -> apply -> 스왑 -> v2026-07-2301 부팅 + sha256 일치 PASS.
  부팅 자동체크 "Already on latest version" 확인. 자산 2종 업로드 완료
- sha256: 3dc2f8584caed8cee70215c2057735bb69e59fff2ee48e65486cff8145ca1dec
### v2026-07-2302 / 2303 (실제 작업일 2026-07-30) — 레퍼런스 호버 확대 + 무조작 자동업데이트 (실바이너리 검증 완료)

**v2026-07-2302 — 두 가지 큰 기능**
1. 레퍼런스 호버 확대 프리뷰
   - 신규 `GET /api/refs/preview/<idx>` — **메모리의 PIL 사본에서 서빙**(경로 기반 아님).
     클립보드/갤러리/업로드/외부파일 등 출처 무관하게 동작하고, 추가 후 원본 파일을 옮기거나
     지워도 계속 보인다(경로 기반이면 404 또는 _is_path_allowed 탈락). PNG라 알파 보존
   - app.js: 220ms 지연 호버(커서 스쳐도 안 번쩍), pointer-events:none(리오더 드래그/Change 드롭 방해 금지),
     드래그·스크롤·클릭·Esc·blur·refreshRefs 리렌더 시 자동 숨김. 캐시버스터를 썸네일과 공유
   - style.css: 체커보드 배경(투명 컷아웃이 패널에 묻히지 않게), 비율 유지(width/height auto)
2. 무조작 자동 업데이트 — **껐다 켜기만 하면 설치됨** (클릭 불필요)
   - launcher `_bg_update_check`가 `auto` 플래그를 이벤트에 실어 보내고 프론트가 즉시 apply
   - **루프 가드**: `~/.nanobanana/auto_update_state.json`에 대상 버전별 시도 횟수 기록.
     AUTO_UPDATE_MAX_ATTEMPTS(2) 초과 시 자동 중단 → 기존 수동 다이얼로그로 폴백
     (AV 격리·동기화 락·읽기전용 폴더처럼 우리가 못 고치는 환경에서 무한 재시작 방지).
     목표 버전 도달 시 상태 파일 삭제 → 다음 버전용 예산 리셋
   - 생성 중(is_generating)이면 자동 설치를 건너뛰고 다이얼로그 표시 → 작업 중 강제 재시작 없음
3. `/api/*` 에러 의미론: 라우팅 오류(404/405)가 트레이스백 500으로 둔갑하던 것 수정.
   HTTPException은 실제 상태코드로, 단 JSON 유지(r.json() 클라이언트 호환)

**v2026-07-2303 — 뷰어 비교 롤백 + 자동업데이트 실검증 릴리스**
- cmpNavigate에 onerror 부재 → 오른쪽 이미지 로드 실패 시 헤더/카운터는 새 이름, 화면은 옛 이미지로
  불일치하던 것 수정. 실패 시 이전 경로로 롤백(_cmpSeq 토큰 가드) + 안내

**검증 (CLAUDE.md 규칙 15 — 업데이트 경로 변경이라 의무)**
- 스왑 기계: v2301 실바이너리 → apply → v2302 부팅 + sha256 일치 PASS
- **핸즈프리**: v2302 EXE를 켜고 **클릭/HTTP 개입 전혀 없이** 16초 만에 v2303으로 자동 교체 + sha256 일치 **PASS**
  (검증 시 auto_update_state.json 초기화해 사용자 최초 조건 재현, /api/events 폴링 금지 준수)
- 라이브: 실행 중 EXE에서 ref preview 200/PNG/900px 서빙 확인, 404가 깔끔한 JSON으로 반환되는 것 확인
- 단위: 시도 카운터 1→2→폴백→성공 시 리셋, 다른 버전 예산 독립 / 프리뷰 파일삭제 후 200·빈슬롯 404·size 가비지 방어

**주의**: 라이브 테스트 중 사용자 ref 슬롯1을 잠시 지웠다가 원위치(슬롯1) 복원 완료.
### ⚠ 버전 태그 날짜 불일치 (2026-07-30 기록)
- **v2026-07-2101 / 2301 / 2302 / 2303은 전부 실제로는 2026-07-30에 릴리스됨.** 태그의 DD가 잘못 찍힘
  (규칙대로였다면 v2026-07-3001~3004). 세션 중 날짜를 오인해 발생
- **기능 영향 없음**: `_version_tuple`은 `\d+`로 뽑아 (2026, 7, 2303) 형태의 숫자 튜플 비교라
  (2026,7,3001) > (2026,7,2303) 이 성립. 다음에 날짜를 바로잡아 v2026-07-3001을 내면
  기존 2303 사용자에게 정상적으로 "최신 버전"으로 감지됨. 월이 바뀌어도 (2026,8,101) > (2026,7,3001) 성립
- **영향은 표기뿐**: 앱 하단 버전 라벨/릴리스 이력에 07-23으로 보임
- **다음 릴리스부터 반드시 오늘 날짜 기준으로 NN 부여** (07-30이면 v2026-07-3001부터)