# NanoBanana 인수인계 — 2026-07-30 현행

이 문서는 **"지금 제품이 어떤 상태인지, 최근 무슨 일이 있었는지, 다음에 뭘 하면 되는지"**를 담습니다.
구조·규칙·배포 절차는 [CLAUDE.md](CLAUDE.md)에, 배포 실행 스크립트는
`.claude/skills/나노바나나api배포/SKILL.md`에 있습니다. (중복 서술 금지 — 여기엔 현황과 이력만)

---

## 1. 현재 상태

- **배포 버전**: `v2026-07-3002`
- **저장소**: https://github.com/productionkhu-tech/freewill-nanobanana (`main` 단일 브랜치)
- **업데이트 방식**: 사용자는 **앱을 껐다 켜기만 하면 자동 설치** (클릭 불필요)
- **프로바이더**: Gemini(Vertex/Studio) · OpenAI gpt-image-2 · Seedream 5.0 Pro/4.5 · Reve 2.1
- **맥 지원**: 소스 실행 방식으로 동작 (`server_mac.py`), Python 3.10+ 필수

### 사용자에게 안내가 필요한 사항
| 대상 | 안내 |
|---|---|
| v2026-06-1201 ~ 07-2001 사용자 | 부팅 체크가 없던 빌드 → **푸터 버전 숫자 클릭 1회** 필요. 이후 완전 자동 |
| Program Files 설치자 | UAC 때문에 자동 업데이트 불가 → 일반 폴더로 이동 |
| Google 결제 미연결 | Studio 429(limit:0) + Vertex 403(BILLING_DISABLED) → 프로젝트에 결제 연결 |
| Reve 402 | 키 문제 아님, 계정 예산 → 콘솔 충전 |
| 맥 사용자 | python.org Python 3.10+ & `Install Certificates.command` 실행 필수 |

---

## 2. 2026-07-30 세션 이력

하루에 6개 릴리스가 나갔습니다. 시간순 요약:

| 태그 | 내용 |
|---|---|
| v2026-07-2101 | 뷰어 단일 프리뷰 비율 왜곡 수정 |
| v2026-07-2301 | Seedream 5.0 Pro 커스텀 최대픽셀 상한 교정 |
| v2026-07-2302 | **레퍼런스 호버 확대** + **무조작 자동 업데이트** + `/api/*` 에러 의미론 |
| v2026-07-2303 | 뷰어 비교 오른쪽 로드 실패 시 롤백 |
| v2026-07-3001 | 릴리스 날짜 라벨 교정 (코드는 2303과 동일) |
| v2026-07-3002 | **릴리스 공개창 404 사고 대응** (draft 우선 업로드 + UpdateNotReady) |

> ⚠ **2101/2301/2302/2303은 실제로는 전부 07-30에 릴리스**됐습니다. 세션 중 날짜를 오인해
> DD가 잘못 찍혔고, 규칙대로면 3001~3004였습니다. 버전 비교가 숫자 튜플이라 기능 영향은 없고
> (3001 > 2303 성립), 3001부터 날짜를 바로잡았습니다. 이미 공개된 태그는 그대로 둡니다.

### 2-1. 뷰어 프리뷰 비율 왜곡 (2101)
프롬프트 푸터(48~160px 가변)가 비동기 갤러리 응답으로 자라며 스테이지 높이를 뺏는데, 창 resize
이벤트가 없어 캔버스 백킹스토어가 줄어든 CSS 박스에 눌려 비율이 틀어졌다(더블클릭하면 정상 복귀).
비교 모드는 자가 치유돼 단일 뷰만 발병. → `#stage`에 ResizeObserver를 걸어 모든 박스 변화에 재fit.

### 2-2. Seedream 커스텀 상한 (2301)
Pro의 Method-2 픽셀 한도는 `2048²×1.1025 = 4,624,220`인데 코드가 그냥 `2048²(4,194,304)`였다.
GPT(8.29M)도 아니고 Seedream 실값도 아닌 근사치라 ~10.25% 짜게 잘림 —
4800x960(4,608,000px, 5:1)이 4576x912로 축소되던 사용자 제보. app.py와 app.js 미러 둘 다 교정.

### 2-3. 레퍼런스 호버 확대 (2302)
신규 `GET /api/refs/preview/<idx>`. **메모리의 PIL 사본에서 서빙**하므로 클립보드/갤러리/업로드/
외부파일 어느 출처든 동일하게 동작하고, 추가 후 원본 파일을 옮기거나 지워도 계속 보인다
(경로 기반이면 404 또는 allowlist 탈락). 220ms 지연, pointer-events:none(드래그/드롭 방해 금지),
드래그·스크롤·클릭·Esc·blur·리렌더 시 자동 숨김, 투명 PNG는 체커보드 위에 표시.

### 2-4. 무조작 자동 업데이트 (2302)
부팅 체크가 `auto` 플래그를 실어 보내고 프론트가 즉시 설치. **시도 예산**(버전별 2회,
`~/.nanobanana/auto_update_state.json`)으로 무한 재시작을 막고, 초과 시 수동 다이얼로그로 폴백.
목표 버전 도달 시 상태 삭제. **생성 중에는 자동 설치를 건너뛴다.**

### 2-5. 릴리스 공개창 404 사고 (3002) — 가장 중요
사용자가 "업데이트 실패: HTTP Error 404"를 제보. 원인은 **GitHub이 릴리스를 만드는 즉시
새 태그를 최신으로 노출하는데 36MB 자산 업로드에 4~5초가 걸리는 것**(전 릴리스 실측).
그 창에 업데이트를 시도한 앱은 없는 파일을 받으려다 404. 자동 업데이트가 켜지며 노출됐고
시도 예산까지 잘못 소모됐다.
- **절차**: 릴리스를 draft로 만들고 자산을 다 올린 뒤 공개 → 무방비 창 **0초** (실측 확인)
- **코드**: 자산 404를 `UpdateNotReady`로 구분해 조용히 넘어가고 **예산 환불**,
  "새 버전을 준비 중입니다" 안내로 표시
- 부수: 시도 예산 관리를 launcher → updater로 이동 (app.py가 launcher를 재import하면 안 되므로)

### 2-6. 맥 지원 보강 (릴리스 외, main 커밋)
- **Gemini 즉사**: Xcode 내장 Python 3.9는 google-genai 1.47까지만 설치 가능한데 그 버전엔
  `ImageConfig.image_size`가 없어 2K/4K 요청이 pydantic 오류로 즉사. → 3.10 미만이면 안내 후 종료
- **Reve만 SSL 실패**: python.org 파이썬의 `Install Certificates.command` 미실행 시 CA 번들이 비어
  표준 urllib 경로(Reve·웹 드래그·릴리스 노트)만 실패. → certifi 번들로 자동 보정
- `requirements_mac.txt`에 `google-genai>=2.8` 핀, 가이드에 필수 단계로 승격
- 맥 고객 키 설치용 붙여넣기 문구 생성(로컬 전용, gitignore) — `~/.nanobanana/`에 keys.env +
  service_account.json 설치. LF 줄바꿈 필수(CRLF면 heredoc 깨짐)

### 2-7. E2E 하네스 함정 (도구 문제, 기록용)
3001 검증에서 "버전은 올랐는데 sha256 불일치" FAIL이 났는데 **제품 결함이 아니라 측정 오류**였다.
사용자 실행본이 포트 5656을 점유 중이라 테스트 바이너리가 조용히 종료되고 **남의 앱을 측정**한 것.
→ 스킬에 사전조건(프로세스 0건 + 포트 미점유)과 "해시 우선 판정"을 규칙으로 추가.
실제 증거는 더 강했다: 사용자 실설치본이 무조작으로 2303 → 3001 자동 교체, 바이트 일치.

---

## 3. 남은 과제

1. **구버전 사용자 공지** (06-12~07-2001) — 푸터 클릭 1회 안내, 아직 발송 안 됨
2. **단일 인스턴스 mutex 레이스** — 1초 내 동시 기동 시 두 창이 뜰 수 있음(2026-07-30 관찰).
   재현되면 mutex 획득~포트 바인딩을 원자적으로 묶어야 함
3. **seedream-4-5 커스텀 상한 검증** — Pro만 교정했고 4-5(현재 4096² 가정)는 문서 대조 미실시
4. **뷰어 자기비교 허용** — 비교 중 좌우가 같은 이미지가 될 수 있음 (무해, 스킵 처리하면 깔끔)
5. **맥 잔여** — Finder "폴더에서 보기" 미지원, 출력 폴더 선택창 불안정(경로 직접 입력 권장)
6. **뷰어 1칸 성능** — 원본 서빙이 저사양에서 무거우면 2048px 고품질 썸네일로 다운시프트

---

## 4. 과거 문서

| 문서 | 시기 | 내용 |
|---|---|---|
| `인수인계_2026-07-20_전체.md` | 07-20 | Reve 에디터 은퇴, 뷰어 개편, 부팅 자동업데이트 복구, 403 폴백 |
| `인수인계_2026-07-15_Reve2.1.md` | 07-15 | Reve 통합~에디터 은퇴 실측 일지 (부분편집 지식 온존) |
| `인수인계_CLAUDE.md`, `인수인계_전체.md` | 그 이전 | 초기 스냅샷 |

위 문서들은 **이력 참고용**이며, 현행 정보는 이 문서와 CLAUDE.md가 우선합니다.

### v2026-07-3101 (2026-07-31) — 갤러리 점프 버튼 + 모델 전환 충돌 수정 (E2E PASS)
- **모델 전환 충돌** (사용자 제보: Gemini 1K → Seedream 이동 시 1K에 묶임)
  - 원인: repopulateSelect가 "새 모델에도 있는 값이면 유지" 규칙 → 이전 해상도가 따라옴.
    실제 MODEL_SPECS로 56가지 전환을 전수 시뮬레이션해 8건 확인
  - 수정: 모델 변경 시 해상도는 **새 모델 기본값으로 리셋**(1K/2K/4K는 모델마다 실제 픽셀이 다른 라벨).
    종횡비는 모델 무관한 '모양'이라 유지, 못 받는 값일 때만 폴백
  - 점검 중 발견한 서버 구멍 2건도 함께 차단:
    · Reve 종횡비 무검증 — REVE_ASPECTS 상수가 정의만 되고 미사용이었음. custom/9:21/1:8 등이
      실제 API 파라미터로 나가 400을 유발할 수 있었음 → 허용목록 통과분만 전송
    · Gemini image_size 무검증 — "gemini-3" 접두 조건에 **3.1-lite(1K 전용)** 도 걸려 2K/4K가
      나갈 수 있었음 → 모델별 tier 허용목록(미등록 모델은 차단 안 함)
- **갤러리 점프 버튼**: 오른쪽 아래 ▲/▼. 숨은 내용 600px 이상일 때만 표시, 래퍼는 pointer-events:none
  (카드 클릭·마퀴 드래그 무영향), 현재 끝에 도달한 쪽은 흐림+비활성
- **부드러운 스크롤**: easeInOutCubic rAF 트윈. 시간 = 240ms + 거리×0.09, **상한 1800ms**
  (1000장 1열 94만px도 1.8초). 프레임당 scrollTop만 써서 카드 수와 무관하게 저비용
  - 안전망 2개: ①프레임 루프 정지 환경에서 150ms 내 첫 프레임이 없으면 즉시 이동(버튼이 죽지 않게)
    ②휠/터치/포인터다운 시 애니메이션 즉시 취소(사용자 조작이 항상 우선 — 이게 없으면 랙처럼 느껴짐)
  - 실제 pywebview 창에서 클릭 후 연속 캡처: 프레임 간 변화율 0.1%→97%→90%→94%로 이징 램프 확인
- **검증**: draft 절차로 릴리스(공개 전까지 latest=3002 유지, 공개 +0.8s에 자산 HEAD 200 — 무방비 창 0초).
  핸즈프리 E2E: v3002 실바이너리 무조작 실행 → 15초 만에 v3101 + 해시 일치 PASS
- sha256: aca7112a67eb2b317b2c4961d820102aa4dd6002cc7d4c7854ebb00c400902d6
- 참고: 태그 날짜는 07-31 기준(직전 3002는 07-30). (2026,7,3101) > (2026,7,3002) 정상 정렬
### v2026-08-0501 (2026-08-05) — 모델 전환 시 "그 모델의 최고 화질"로 (E2E PASS)
- **제보**: 1K/2K만 되는 모델을 쓰다가 4K 되는 모델로 가면 하위 화질에 갇힘. 근데 Custom을 쓰면 2K/4K가 됨
- **원인 2가지**
  1) v2026-07-3101에서 "모델 바꾸면 그 모델 defaultResolution으로 리셋"으로 고쳤는데, 그 기본값들이
     보수적이었음 — G3 Pro 2K / G3.1 Flash 2K / SD 4.5 2K / **gpt-image-2 1K** (넷 다 4K 지원).
     그래서 4K 모델로 가도 낮은 등급에서 시작
  2) Custom 종횡비 사용 중에는 Resolution 드롭다운을 비활성+흐림 처리했는데, 낡은 "1K"가 회색으로
     남아 **저화질에 갇힌 것처럼** 보였음. custom 지원 모델끼리(SD↔GPT) 이동하면 계속 유지돼 더 헷갈림
- **수정**
  1) 모델 전환 시 **그 모델이 낼 수 있는 최고 등급** 선택. 배열 순서가 아니라 명시적 랭크
     (512px<1K<2K<4K)로 판정, 랭크 없는 값(Reve "auto")은 마지막 항목 폴백.
     이 규칙이 07-30 제보(1K 고착)와 08-05 제보(4K 모델인데 하위)를 동시에 만족하는 유일한 규칙.
     **프로젝트/갤러리 Load는 영향 없음**(저장값 그대로 복원)
  2) Custom 사용 중엔 드롭다운을 숨기고 "Custom 크기 사용 중" 안내로 교체. select 값은 그대로 유지해
     saveSettings가 항상 유효한 등급을 보냄
- **검증(실앱)**: SD 5.0 Pro→2K, SD 4.5→4K, GPT→4K, G3.1 Flash→4K, G3 Pro→4K, 1K전용 모델은 1K.
  Custom 진입→안내문 교체, 모델 이동해도 유지, Custom 해제→드롭다운 복귀(4K)
- sha256: a6792fa3544a07c9d5888690aa0f7389912cc53e289efcbbf346a93f1d0a9543

### ⚠ E2E 함정 추가 (2026-08-05) — 스킬 반영 완료
- 첫 E2E가 FAIL(자동 업데이트 안 일어남)이었는데 **제품 결함이 아니라 측정 오류**.
  브라우저 패널을 http://127.0.0.1:5656 에 열어둔 채여서 **/api/events 를 800ms마다 폴링해
  update_status 이벤트를 가로챔** → 앱 창이 못 받아 자동 설치가 안 됨 (CLAUDE.md에 적힌 그 함정을
  테스트 도구가 그대로 밟음). 패널을 다른 주소로 옮기고 재실행하니 18초 만에 PASS
- 사전조건에 "프로세스 0건 + 포트 미점유"에 더해 **"/api/events 를 보는 창이 없을 것"** 추가

---

## 2026-08-31 릴리스 (v2026-08-3101 → 3103)

- **3101**: 모델 표시/시작화질 설정 (Model 옆 톱니). 앱 전역 prefs.json 저장 - 재시작·업데이트·전 탭 공유.
  숨김은 목록 필터일 뿐(숨긴 모델 프로젝트도 정상 동작). 화질 우선순위: 로드값 > 모델별 설정 > 최고.
  새 모델은 MODEL_SPECS 한 곳에 스펙만 추가하면 맨 아래 자동 등록 (서버 목록 _ALL_MODEL_IDS 도 한 줄).
  `_prefs_file()` 이 NANOBANANA_DATA_DIR 를 무시하던 격리 구멍 수리 (테스트가 실제 prefs.json 오염 - 3번째 사고).
- **3102**: 자동 업데이트가 간헐적으로 침묵하던 원인 수리 - update_status/progress/swap 이 프로젝트
  pid 로 스탬프되어, 부팅 pid 변동(시작탭→복원→제거) 경합에서 프론트 pid 필터에 버려졌음.
  이벤트를 앱 소속(pid None)으로 + 프론트 필터 면제. 0701부터 잠복, 2801의 부팅 키 갱신(네트워크)이
  경합 창을 넓혀 표면화.
- **3103**: 창의 첫 프로젝트 조회가 세션 복원과 경합하면 activePid 가 낡은 채 고정되던 문제 -
  부팅 후 3s/8s 재동기화. E2E: 3102→3103 핸즈프리 20초 PASS (sha256 일치).
- **주의**: 2801/3101 을 쓰는 기기는 구버전 프론트 경합 탓에 이번 점프가 복불복 - 부팅마다 재시도되며,
  안 되면 푸터 버전 클릭 1회 (릴리스 노트에 안내 포함). 3102 부터는 결정적.
- 키 서버(Cloudflare Workers) 가동 중: 구키=입장권, 등록창구 ENROLL_OPEN=1 열림 상태.
  전원 등록 확인 후 `wrangler deploy --var ENROLL_OPEN:0` 로 닫을 것.


## 2026-09-01 릴리스: v2026-09-0101 — Reve 완전 제거

Reve가 API 서비스를 공식 종료해 옵션을 전부 들어냈다 (순삭제 595줄).

- 제거: MODEL_SPECS 항목(드롭다운·설정 모달은 여기서 파생), 상태 점등, 레퍼런스 예산 게이지,
  누끼 토글, 생성 경로, 레이트리미터, 클라이언트 초기화, 그리고 2026-07-20 에디터 은퇴 때
  고아가 된 레이아웃 편집 헬퍼들(라우트·프론트 참조 0건 확인 후 삭제)
- 레거시 안전장치: `_RETIRED_MODEL_FALLBACK` + `_live_model_name()` — state.model을 저장물에서
  세팅하는 3지점(프로젝트 ui_state 로드, /api/settings POST, 갤러리 setup 적용)에서 reve-create를
  기본 모델로 폴백. `_MODEL_RENAMES`(개명 맵)에 합치지 않은 이유: 그 맵은 model-prefs 키도
  정규화해서 합치면 "Reve 숨김"이 "기본 모델 숨김"으로 변질된다. prefs의 reve 잔재는
  unknown-plausible로 무해하게 유지(v3101 설계), 전부-숨김 가드는 Reve를 더는 세지 않음
- 표시는 정직하게: Reve로 만든 옛 이미지의 갤러리 라벨("Reve 2.1")·[R] 배지·핑크 CSS는 유지
- 검증: 격리 환경(NANOBANANA_DATA_DIR) 18/18 통과 — 구 Reve 프로젝트 오픈+리맵(비율/장수 보존),
  스테일 프론트 settings POST 리맵, status/settings에 reve 필드 없음, generate 라우트 무결,
  prefs 왕복, 세션 저장
- 함께 커밋: 08-31에 배포만 되고 커밋이 빠졌던 Cloudflare Worker app_version 추적(worker.js),
  배포 스킬에서 Reve 키 주입 단계 제거


## 2026-09-01 릴리스: v2026-09-0102 — GPT 키 2차 교체 + 비교버튼 CSS 복구 + ref 우클릭 복사

### GPT 키 2차 교체 (워커 시크릿만 수정, 앱 코드 변경 없음)
- `OPENAI_KEY` = 신규 svcacct 키(지문 312fadf6d835)로 교체. **넣기 전에 models.list 로 실동작 확인**
  (gpt-image-2 포함 124개 모델) — 죽은 키를 배포하면 70대가 조용히 구키에 머문다
- `TICKET_HASHES` = **구 sk-proj + 1차 교체분 svcacct + 신규 svcacct 3개 해시**.
  1차 교체를 받은 PC 는 환경변수에 이미 svcacct 가 들어 있어서, 그 해시가 없으면
  재설치/새 PC 때 등록이 막힌다. 신규 키 해시까지 넣는 건 worker.js 주석의 설계 그대로
  ("지금까지 발급된 모든 키를 티켓으로 유지")
- 검증: 세 키 모두 /enroll 200, 가짜 키 403, 테스트 토큰 3개는 즉시 revoke.
  이 PC 에서 실제 refresh 돌려 ef9314a7e9f2 -> 312fadf6d835 교체 + 레지스트리 영속 확인
- **이미 등록된 PC 는 티켓이 아니라 토큰으로 /key 를 받으므로 앱만 켜면 끝**

### ★ wrangler 인증 함정 (이번에 30분 소모)
`CLOUDFLARE_API_TOKEN` 환경변수가 설정돼 있으면 wrangler 가 **OAuth 세션보다 그걸 우선**한다.
그 토큰엔 Workers 권한이 없어서 secret put 이 403(code 10000). whoami 는 오류 핸들러가 부르는
거라 "account ID 조회 실패" 라는 엉뚱한 메시지만 보인다 — **진짜 원인은 로그 파일 앞부분**.
해결: `env -u CLOUDFLARE_API_TOKEN npx wrangler ...` 로 OAuth 세션(`~/AppData/Roaming/xdg.config/.wrangler/config/default.toml`) 사용.

### ★ CSS 회귀 (v2026-09-0101 에 실려나감, 사용자 제보)
Reve CSS 제거 스크립트가 규칙 위 주석을 지우면서 **두 줄 주석의 닫는 줄만** 삭제 → `/*` 가 열린 채
남아 바로 아래 `.cmp-btn` 2줄을 통째로 삼켰다. 스타일이 하나도 안 먹어 브라우저 기본 버튼
(Arial 13.3px)으로 보였다. 삭제 전 원본에서 **주석을 원자 단위로 다루는 토크나이저**로 재생성.
규칙 단위 대조로 334개 · 손실 0 · 변경 0 · 추가 0 확인 (깨진 빌드는 332개 = 정확히 그 2줄).
교훈: **CSS 를 줄 단위 필터로 편집하지 말 것.** 주석/블록 경계를 아는 파서로만.

### ref 우클릭 복사 + 중복 첨부 봉합
- 레퍼런스 셀 우클릭 → 원본 해상도로 클립보드 복사. 슬롯 **인덱스**로 접근하고 메모리 PIL 에서
  서빙 → 클립보드 붙여넣기 ref 나 원본 파일이 사라진 ref 도 동작 (/api/refs/preview 와 같은 이유)
- 윈도우 DIB / 맥 pasteboard 기록을 갤러리 복사와 **하나의 헬퍼로 통합**. 맥 경로는 이제 PNG 를
  직접 인코딩 — 기존엔 JPEG 바이트를 PNG 라고 태깅해 pasteboard 에 올리고 있었다
- **중복 구멍**: 클립보드를 거치면 재인코딩돼 SHA1 파일명 dedup 이 못 잡는다(실측: 1200x900 JPEG
  이 다른 SHA1, 동일 픽셀버퍼로 돌아와 슬롯 2개 차지). 신규 기능 이전부터 있던 구멍(갤러리 복사
  → Ctrl+V 도 동일). `add_ref_image` 가 **디코드된 픽셀**을 비교하도록 수정, 크기+모드 선필터라
  평소 추가는 픽셀 연산 0 (miss 0ms / 진짜 일치 27ms)
- **명시 슬롯(프로젝트·setup 로드)은 면제** — 저장물이 슬롯을 결정하므로 거기서 빼면 이미지 손실
- 거절 사유 전달: `add_ref_image_ex` 가 (ok, reason) 반환, 경로로 추가하는 3개 라우트가 노출

### setx 출력 디코딩 제거
한국어 Windows 에서 setx 는 cp949 로 답하는데 `text=True` 로 캡처해 인터프리터 기본이 UTF-8 이면
subprocess 리더 스레드에서 UnicodeDecodeError. 출력은 애초에 안 읽으므로 DEVNULL 로.

### 남은 일
- `ENROLL_OPEN=1` 아직 열려 있음. 전원 이관 확인 후 `wrangler deploy --var ENROLL_OPEN:0`
  (반드시 `env -u CLOUDFLARE_API_TOKEN` 붙일 것)
- 채팅으로 오간 키 3개는 노출된 값 — 게이트웨이가 있으니 교체는 워커 시크릿 한 줄이면 된다


## 2026-09-09 릴리스: v2026-09-0901 — GPT Image 2.5 (Flare / Sunburst)

- 모델 목록: gpt-image-2 아래 **Flare (Fast)** → **Sunburst (Heavy)** 순. 둘 다 기본 화질 **max**
  (gpt-image-2 가 자기 최고치인 high 로 시작하는 것과 같은 규칙)
- 크기 규칙이 gpt-image-2 와 자릿수까지 동일 → `_gpt2_*` 헬퍼·종횡비·커스텀 픽셀 전부 재사용.
  새로운 건 화질 사다리(xhigh, max)뿐
- **숨어 있던 버그**: `_generate_one_image_openai` 가 model 인자를 안 받고 GPT2_MODEL_ID 를
  하드코딩. OpenAI 모델이 하나일 땐 안 보이다가 셋이 되는 순간 틀린다 → model 을 받아 전달
- **quality 검증 신설**: `openai_clamp_quality()` — 2.5 에서 max 쓰다 gpt-image-2 로 돌아가면
  400(5회 재시도 후 실패)이 나던 경로. 모델이 모르는 값은 high 로 누른다
- **프론트**: 화질 목록이 `applyModelSpec` 안에 하드코딩돼 있어 그대로 두면 xhigh/max 가
  안 보였다 → `MODEL_SPECS[].qualities` 로 이동. 또 `repopulateSelect` 는 기존 선택값이
  새 모델에서도 유효하면 유지하므로, 2.5 로 가도 high 에 머물렀다 → `preferredQuality()` 를
  `preferredResolution()` 과 같은 방식으로 만들어 onModelChange 가 명시 전달
- 검증: 스텁 27/27 + 회귀 8/8, **SDK 가 전선에 싣는 JSON 을 가로채** 세 모델 모두
  선택==전송 확인, 스테일 max→high 클램프 확인, 두 모델 실제 생성 성공
  (max 기준 Flare 38초 / Sunburst 89초). 자동 업데이트 E2E(0102→0901) PASS
- **조직 인증 필요**: 미인증이면 403. 인증 후 전파에 시간이 걸리며 그동안 간헐 403 이 난다.
  `is_retryable_error` 에 403 이 없어 그 구간엔 하드 실패 → 전파 20/20 확인 후 배포했다

### ★ 테스트가 사용자 프로젝트를 오염시킨 사고 (4번째, 이번 세션)
`shared.project_default_save_dir` 를 `init_app()` **뒤에** 설정해서, init_app 의 세션 복원이
실제 `~/Documents/NanoBanana JSON/` 를 스캔해 사용자의 띵크웨어 프로젝트를 열어버렸다.
이후 테스트 생성 24장이 그 프로젝트에 쌓이고 autosave 로 실제 파일에 기록됐으며,
`/api/settings` POST 가 model/aspect/resolution/quality/count/output_dir/prompt_sections 를
덮었다(출력 폴더가 곧 삭제될 %TEMP% 를 가리키는 상태였음).
복구: 파일 경로가 전부 %TEMP% 하위라 `/api/gallery/delete` 로 정확히 24개만 제거(219→195),
ui_state 는 가장 최근 정상 이미지의 `generation_settings` 로 복원(seedream-5-0-pro / 16:9 /
2K / count 2 / Desktop 의 NanoBanana_Output / prompt_sections [""]), fixed_prompt("강아지")은
사용자가 직접 바꾼 값이라 유지. **NANOBANANA_PROJECT_DIR 은 존재하지 않는다** — app.py 는
NANOBANANA_DATA_DIR 만 읽으며 그건 session/prefs/update-state 만 덮는다.


## 2026-09-09 릴리스: v2026-09-0902 — GPT Image 2.5 투명 배경(알파 PNG)

- 체크박스 `Transparent background` (화질 아래). **기본 꺼짐**, 2.5 두 모델에서만 노출
- API 는 `background:"transparent"` + `output_format:"png"` 를 같이 보내야 알파가 남는다.
  gpt-image-2 는 이 파라미터를 거부하므로 **2.5 이면서 체크됐을 때만** 실린다
- 값은 **프로젝트별**(`AppState.openai_bg_transparent`, `_Shared` 아님).
  ui_state(프로젝트 저장/열기) · job 스냅샷(생성 중 탭 전환해도 큐에 넣을 때 설정 유지) ·
  generation_settings(갤러리 카드 Load 로 프롬프트와 함께 복원) 세 곳에 실린다
- 모델을 바꾸면 체크박스와 **서버 플래그를 둘 다** 끈다. 행만 숨기면 400 나는 모델에
  투명 요청이 남는다

### 표시 (두 군데가 비어 있었다)
- `/api/gallery/thumb` 이 `_to_rgb_flatten` + JPEG 라 누끼가 **흰 박스**로 보였다 →
  원본에 알파가 있을 때만 PNG 로 서빙(나머지는 기존 JPEG 성능 유지)
- 어두운 카드 위에선 투명과 "어두운 배경"이 구분이 안 된다 → 체커보드.
  카드는 `.card-img.alpha` 클래스로 **해당 이미지에만**(안 그러면 일반 카드 레터박스에도 깔림),
  뷰어는 drawImage 직전에 **이미지 사각형 안쪽만** 칠한다(불투명 이미지는 완전히 덮여 무영향).
  격자 크기·색은 두 곳이 동일

### ★ 측정해서 알아낸 것 — 파라미터만으론 누끼가 안 된다
프롬프트가 `사과` 한 단어면 배경 있는 장면을 그리고 그 위에 흐릿한 매트를 씌운다
(완전투명 0%, 전 픽셀 알파 2~251). `isolated, no background` 처럼 피사체를 지정하면
완전투명 46~64% 의 진짜 누끼가 나온다. **1K·4K 모두 동일하므로 해상도 문제가 아니다.**
그래서 체크했을 때만 뜨는 안내 문구를 넣었다.
또 하나: 2.5 는 피사체 알파를 **255 가 아니라 250~254** 로 돌려준다(전 케이스 공통).
눈으로는 불투명. 임의로 255 로 밀면 머리카락·유리 같은 진짜 반투명 경계가 망가지므로
모델이 준 값 그대로 저장한다

### 검증
4개 스위트 54/54 — 투명 전 구간(전선 파라미터, 저장 파일 RGBA+실제 구멍, 썸네일 알파,
목록 플래그, 프로젝트/setup 왕복), 탭 격리 9종(새 탭 기본 꺼짐, 두 탭 불일치 무누수,
생성 중 탭 전환해도 스냅샷 유지), 2.5 라우팅 27, 기존 회귀 8.
자동 업데이트 E2E(0901→0902) PASS, 부팅 체크 스레드 정상.

### 클립보드 제약 (안내 필요)
우클릭 복사는 윈도우 CF_DIB 라 알파가 없다 → 투명이 흰색으로 채워진다.
투명을 살리려면 파일을 그대로 써야 한다(Explorer 버튼). 클립보드에 PNG 형식을 함께
올리는 방법이 있으나(포토샵·피그마가 읽음) 별도 작업.


## 2026-09-09 릴리스: v2026-09-0903 — 클립보드 알파 보존

- 누끼를 복사하면 어디에 붙여넣어도 흰 배경이 채워졌다. 원인은 클립보드에 **CF_DIB 하나만**
  올렸기 때문 — 그 형식엔 알파 채널이 없다
- 윈도우 클립보드는 **한 번의 복사에 여러 형식을 동시에** 담을 수 있고, 브라우저가 웹의 PNG를
  복사할 때 하는 게 정확히 그것이다: CF_DIB + 등록 형식 `"PNG"`. PNG 를 아는 앱(포토샵·피그마·
  디스코드·오피스)은 뒤엣것을 집어 투명을 살리고, 모르는 앱은 앞엣것을 쓴다
- `_clipboard_put_pil` 이 이제 둘 다 올린다. **CF_DIB 가 1순위로 그대로 남아** 기존 붙여넣기
  경로는 무변화, PNG 는 `RegisterClipboardFormatW("PNG")` 로 얹기만 한다.
  PNG 등록/할당 실패는 best-effort — 투명만 포기하고 복사 자체는 성공시킨다.
  `SetClipboardData` 가 소유권을 안 가져간 모든 경로에서 핸들을 해제한다
- 갤러리 카드·레퍼런스 슬롯 우클릭이 같은 헬퍼를 쓰므로 둘 다 적용. 맥은 원래 pasteboard 에
  PNG 를 쓰고 있어 영향 없음
- 검증: 실제 클립보드를 읽어 `CF_DIB, PNG, CF_BITMAP, CF_DIBV5` 확인(뒤 둘은 윈도우가 합성),
  PNG 항목이 PNG 시그니처로 시작하고 디코드하면 RGBA + 원본과 동일한 투명 7800px.
  ImageGrab(기존 DIB 경로)도 그대로 동작
- **제약**: 붙여넣는 쪽이 PNG 형식을 읽어야 투명이 산다. 그림판처럼 CF_DIB 만 보는 앱은
  여전히 흰 배경 — 앱 쪽 한계이지 우리 버그가 아니다
- 투명 배경 안내 문구를 한 절로 줄였다 ("피사체를 지정해야 잘립니다 — "isolated, no
  background" 처럼."). 뒷문장이 한국어로 어색했고 앞절만으로 할 일이 전달된다

배포 전 점검 76개 통과 — 클립보드 7 / 투명 10 / 탭격리 9 / 2.5 라우팅 27 / 기존회귀 8 /
실행 중 앱 읽기전용 스모크 15(드롭다운 순서, 체커보드 CSS, `.cmp-btn` 이 주석 밖에 있는지 포함).
자동 업데이트 E2E(0902→0903) PASS.

## 2026-09-16 릴리스: v2026-09-1601 — 팀/프로젝트별 사용량·비용 집계

"어느 건에 얼마 썼는지" 를 알고 싶다는 요구. 앱은 귀속을 강제하고, 집계·조회는 밖(관리자 페이지)에서 한다.

### 앱 쪽
- 귀속은 **프로젝트별 상태**(`AppState`)다. `_Shared` 가 아니다 — 한 창에 여러 건을 띄워놓고
  일하는 게 정상이라 탭마다 다른 팀·프로젝트여야 한다
- `billing_confirmed` 는 **저장하지 않는다**. 팀/프로젝트 값은 프로젝트 파일에 살아남지만
  "확인" 은 세션 한정이라 앱을 껐다 켜면 무조건 다시 고르게 된다 (요구사항)
- 생성 직전 `_billing_still_valid()` 로 목록에 아직 있는지 다시 본다. 관리자가 프로젝트를
  내리면 그걸 잡고 있던 사람은 다시 고르라는 안내를 받는다. **목록을 못 받아오면 통과시킨다** —
  집계를 지키려다 일을 못 하게 만들 수는 없다
- job 스냅샷에 귀속을 함께 담는다. 생성 중에 탭을 옮겨도 배치는 시작할 때의 건으로 잡힌다
- `_extract_usage()` 가 프로바이더 응답에서 토큰·장수·출력 픽셀을 뽑아 `usage_spool.jsonl` 에 쌓고
  30초 데몬이 모아 보낸다. **스풀이 비면 네트워크를 아예 안 탄다.** 이벤트 id 는 앱이 만들고
  서버가 `INSERT OR IGNORE` 로 받아 재전송이 중복이 되지 않는다
- **누가 보냈는지는 앱이 주장하지 못한다.** 사용자·장비는 게이트웨이 토큰에서 서버가 읽는다

### 게이트웨이(Cloudflare Worker + D1)
- `/catalog`(토큰) · `/usage`(토큰, 배치·멱등) · `/admin/*`(ADMIN_KEY). 관리자 페이지는 `/admin`
- **금액을 저장하지 않는다.** 원본 사용량만 쌓고 단가는 조회 시점에 조인한다 —
  단가를 잘못 넣었을 때 과거까지 한 번에 바로잡는 유일한 방법
- 단가는 `effective_from` 으로 시점을 나눈다. 새 단가로 행을 덮어쓰면 지난달 집계가 조용히 변한다
- `seedream-5-0-pro` 는 픽셀 구간제: 2.61M px 이하 $0.045 / 초과 $0.09 (콘솔 실측값.
  4.0 의 $0.03 을 쓰면 틀린다). 입력 이미지는 2장째부터 장당 $0.003
- 모르는 모델이 들어오면 `prices` 에 `verified=0` 으로 자동 등록된다 — 새 모델이 나와도
  집계가 멈추지 않고, 관리자 페이지에 "단가 확인 필요" 로 뜬다
- 하루 한 번 크론으로 환율을 받아 KRW 환산에 쓴다
- **프로젝트는 팀에 묶지 않는다**(`decouple.sql`). 한 건을 여러 팀이 같이 하는 게 정상이라
  프로젝트에 팀을 하나 박으면 엉뚱한 팀으로 비용이 잡힌다. `usage_events` 가 둘을 따로 들고 있다

### UI
- Billing 컨트롤을 사이드바 **맨 위**에, 바로 아래 Model 드롭다운과 같은 치수·색으로.
  화살표는 글리프(▾)가 아니라 CSS 로 그렸다 — 글리프는 네이티브 select 보다 작고 흐려서 따로 놀았다
- 안 고른 탭은 테두리·글자가 강조색(`선택 필요`). 누르면 바로 바꿀 수 있다
- 탭에 붙이던 작은 라벨(`.pt-bill`)은 **제거**. 탭 스트립이 지저분해졌고 사이드바와 중복이라
  탭 툴팁으로 옮겼다

### 검증
배포 전 120개 통과 — 귀속 15 / 탭격리 9 / 투명 10 / 2.5 라우팅 27 / 기존회귀 8 / 클립보드 7 /
회수(내려간 프로젝트) 7 / 게이트웨이 사용량 20 / 실행 중 앱 스모크 17.
자동 업데이트 E2E(0903→1601) **PASS** — 구버전을 켜놓고 아무 조작 없이 18초 만에 교체,
교체된 파일의 sha256 이 릴리스본과 일치.

### 테스트 하네스 수정 (앞으로 주의)
- 귀속 게이트가 생기면서 **가짜 팀/프로젝트 id 를 쓰던 기존 테스트가 전부 막혔다.**
  앱 버그가 아니라 테스트가 서버 실데이터에 기대고 있었던 것 — 이제 `A._nbgw.fetch_catalog` 를
  고정 목록으로 스텁해 독립시켰다
- 게이트웨이 테스트는 이벤트 id 를 고정하고 1999-01-01 날짜에 묻으며 끝나면 항목을 내린다.
  안 그러면 돌릴 때마다 D1 에 쓰레기가 쌓이고 사용자 집계 기간에 가짜 비용이 섞인다
- 스모크는 갤러리 장수·탭 구성 같은 "사용자가 쓰면서 변하는 값" 을 더 보지 않는다. 버전은 인자로 받는다

### 남은 것
- `gemini-2.5-flash-image` 단가가 `verified=0` (공식 표에서 못 찾음). 관리자 페이지에서 채워야 정확해진다
- 게이트웨이 `ENROLL_OPEN=1` 이 여전히 열려 있다 — 배포가 끝나면 닫는 것을 권장
- `gateway/admin_page.js` · `usage_module.js` 는 worker.js 에 인라인되면서 쓰지 않는 스크래치다(미커밋)

## 2026-09-16 릴리스: v2026-09-1602 — 비용 계산 정정 · 보관 · 읽히는 리포트

집계를 붙인 다음 날 바로 나온 피드백: "로우 데이터만 묶어 보는 느낌이라 남들과 같이 볼 때
'오 이 정도구나' 가 안 된다", "프로젝트가 끝나서 끄면 옛날 가격을 못 보는 게 너무 오차다",
"값의 오류는 당연히 있으면 안 된다". 셋 다 맞는 지적이었고, 특히 세 번째가 실제로 틀려 있었다.

### 값이 틀렸던 세 군데 (전부 실제 오류였음)
1. **`usage_events` 에 `out_px` / `ref_images` 칸이 없었다.** 앱은 처음부터 보내고 있었는데
   워커의 INSERT 에 자리가 없어 그대로 버려졌다. 결과로 seedream-5-0-pro 의 픽셀 구간제
   (2.61M px 초과 $0.09)가 **한 번도 적용된 적이 없고** 4K 이미지가 전부 절반 값으로 잡혔다.
   레퍼런스 2장째부터 붙는 $0.003 도 마찬가지. `gateway/usage2.sql` 로 칸을 추가했다.
   **지난 행은 0 으로 남는다** — 그 값을 지금 되살릴 방법이 없다. 이후 행부터 정확하다
2. **단가 조인이 `p.model = e.model` 뿐이었다.** `prices` 의 PK 는 `(model, effective_from)` 이라,
   단가를 한 번이라도 바꾸면 이벤트 한 행이 단가 두 행에 매칭돼 **비용이 그대로 두 배**가 된다.
   시점을 나눠 둔 이유가 그대로 폭탄이 될 뻔했다. 이제 "그 날짜 이하 중 가장 최근" 한 행만 고른다
3. **원화 환산이 최신 환율 하나로 전체를 곱하고 있었다.** 설계 주석엔 "그 날 환율" 이라고
   써 놓고 구현은 `latestFx()` 하나였다. 지난달 숫자가 매일 조금씩 달라지는 상태였다.
   이제 `fx_rates` 를 이벤트 날짜로 조인하고, 없으면 직전 영업일, 그것도 없으면 가진 것 중
   가장 오래된 값으로 메운다(오늘 값으로 메우면 또 매일 달라진다)

덤으로 `/admin/prices` POST 도 두 가지가 틀려 있었다: `WHERE model` 로 UPDATE 해서 **과거 시점
행까지 같이 덮었고**, `per_image_hi` / `px_threshold` / `in_per_image` 는 화면에 칸만 있고 저장이
안 됐다. 이제 `(model, effective_from)` 단위 upsert 이고, "이 행 수정"(오타 정정)과
"오늘부터 새 단가"(시점 열기)가 버튼으로 갈라져 있다.

### 보관 — 끈다고 과거가 사라지면 안 된다
- `active=0` 은 **삭제가 아니라 보관**이다. 앱의 선택 목록에서만 빠지고 사용량·이름표는 남는다.
  집계 쿼리는 처음부터 active 로 안 걸렀지만, `/admin/catalog` 가 살아 있는 것만 돌려줘서
  **관리자 화면에서 보관된 게 아예 안 보였다** — 그래서 "서버 설정을 건드는 느낌" 이 맞았다.
  이제 보관된 것까지 내려주고 "보관됨 N개" 로 따로 묶어 **되살리기**를 제공한다
- 리포트 행에는 `archived` 플래그가 붙어 "보관" 배지로 표시된다. 지난 비용은 그대로 보인다
- 오타로 만든 항목만 **삭제**할 수 있다. 사용 기록이 한 건이라도 있으면 서버가 거부한다 —
  지우면 이름표가 사라져 리포트에 슬러그만 남고, 이 집계의 존재 이유가 무너진다

### 목록 정렬
SQL 의 `ORDER BY name` 은 문자열 비교라 **10팀이 1팀보다 앞에** 왔다. 숫자 덩어리는 숫자로
비교하는 자연 정렬을 `handleCatalog` 에 넣었다 (현대 한글 음절은 코드포인트 순서가 가나다
순서와 같아 별도 로케일 데이터가 필요 없다). 앱과 관리자 화면이 같은 순서를 본다.

### 관리자 페이지 — 리포트답게
- 기간 프리셋(이번 달/지난달/최근 7일/30일/올해/전체)과 **"2026년 9월 1일 ~ 9월 16일 (16일간)"**
  한 줄. 남과 같이 볼 때 제일 먼저 나오는 질문이 "언제부터?" 라서 제일 크게 뒀다
- KPI 카드 4장(총 비용/이미지/하루 평균/단가 미설정) → 날짜별 막대 그래프 → 비중 막대 → 상세 표
- **그래프와 표가 같은 비용식 문자열(`COST_USD`)을 공유한다.** 두 벌로 적으면 언젠가 한쪽만
  고쳐져 합계와 그래프가 어긋난다. 테스트가 둘의 합계 일치를 검사한다
- 기록이 없는 날도 막대 자리를 차지한다(기간이 120일 이하일 때). 있는 날만 그리면 간격이
  날짜와 어긋나 읽을 수 없다
- 라이브러리 없이 SVG 로 직접 그렸다 — 워커가 외부 스크립트를 끌어올 이유가 없다

### 앱
- 사이드바 섹션 제목 `Billing` → `Status`
- **모달을 먼저 띄우고 목록은 뒤따라 받는다.** 예전엔 클라우드 왕복이 끝나야 창이 나타나
  누를 때마다 버벅였다(2초 → 60ms). 받는 동안은 입력을 막는다 — 캐시가 다른 탭 값일 수
  있어 그 사이 Enter 가 통하면 엉뚱한 프로젝트로 확정된다
- **마지막에 고른 팀/프로젝트를 prefs 에 남겨 다음 실행에 미리 골라둔다.** 확인은 여전히
  매번 받지만(요구사항), 17개 팀 × 7개 프로젝트를 매번 처음부터 찾게 할 이유는 없다.
  Enter 한 번이면 확정. 프로젝트 파일에 저장된 값이 있으면 그게 우선이다
- Enter = 적용 / Escape = 닫기

### 테스트가 운영 집계를 오염시키고 있었다
`init_app()` 이 사용량 업로더를 띄우는데 테스트도 `init_app()` 을 부른다. 그래서 테스트가
만든 가짜 사용량이 20초 뒤 이 PC 토큰으로 **운영 D1 에 올라가고 있었다** (qa-proj 같은 행이
실제로 섞여 있었다). 이제 `NANOBANANA_DATA_DIR` 이 잡혀 있으면 올리지 않는다 — 테스트가
어차피 반드시 세팅하는 변수라 잊어버릴 수가 없는 안전장치다. 진짜로 폴더만 옮긴 경우엔
`NANOBANANA_USAGE_UPLOAD=1` 로 되살린다.
섞여 들어갔던 9건과 테스트용 팀/프로젝트 11건은 지웠다. 현재 운영 D1 은 17팀 / 7프로젝트 /
사용 2건(사용자가 직접 돌린 TA Test)뿐이다. 게이트웨이 테스트를 돌린 뒤에는
`gateway/purge_test_rows.sql` 로 흔적을 치울 것.

### 검증
배포 전 129개 통과 — 귀속 18 / 탭격리 9 / 투명 10 / 2.5 라우팅 27 / 기존회귀 8 / 클립보드 7 /
회수 7 / 게이트웨이 사용량 20 / **비용 계산 18(신규)** / 실행 중 앱 스모크 19.
새로 만든 `test_cost.py` 가 픽셀 구간제, 레퍼런스 과금, 시점별 단가(과거 불변 + 2배 방지),
보관 후에도 이름표 유지, 기록 있는 건 삭제 거부, 그래프 합계 = 표 합계를 검사한다.
자동 업데이트 E2E(1601→1602) **PASS** — 구버전을 켜놓고 조작 없이 20초 만에 교체, sha256 일치.

### 남은 것
- `gemini-2.5-flash-image` 단가가 여전히 `verified=0` (공식 표에서 못 찾음)
- 게이트웨이 `ENROLL_OPEN=1` — 배포가 끝나면 닫을 것
- 이번 마이그레이션 **이전** 에 쌓인 사용 행은 `out_px=0` 이라 픽셀 구간이 적용되지 않는다
  (해당 기간 seedream 고화소 비용은 실제보다 낮게 잡혀 있다). 지금은 2건뿐이라 영향 없음

## 2026-09-16 릴리스: v2026-09-1603 — 팀/프로젝트 창은 빈 칸으로 뜬다

v1602 에서 "매번 고르기 귀찮다" 를 편의로 풀겠다고 지난번 값을 **미리 골라두게** 했는데,
바로 반박이 들어왔다: *"처음에 탭 열거나 팀·프로젝트 할 때 이미 뭔가 입력되어 있는 건
좋지 않지 않나, 뭐 선택… 이런 느낌으로 떠 있어야 하는 거 아닌가."*

맞는 지적이다. 고를 칸에 뭔가 들어 있으면 읽지 않고 확정한다. 그러면 **이 창을 매번 띄우는
이유 자체가 사라진다** — 비용을 어디에 달지 의식적으로 고르게 만드는 게 목적이었는데
"Enter 한 번" 이 그 의식을 없애 버린다. 편의가 목적을 먹은 셈이라 되돌렸다.

- 두 드롭다운의 첫 칸은 비어 있다(`선택…`, disabled). 둘 다 고르기 전에는 적용 버튼이
  꺼져 있고 Enter 도 통하지 않는다 — 눈으로도 못 누르는 게 보인다
- **이미 확정해 둔 탭을 다시 열 때만** 그 탭이 고른 값을 되살린다. 그건 "미리 골라주기" 가
  아니라 "지금 상태 보여주기" 다
- 지난번에 쓰던 팀·프로젝트는 아래 한 줄로 **제안만** 한다. `이대로 채우기` 를 눌러야
  들어간다 — 채우는 건 사용자의 손이어야 한다. prefs 에 남기는 것 자체는 v1602 그대로
- 포커스는 고를 게 남았으면 팀 드롭다운, 다 골랐으면 적용 버튼

### 고치다 발견한 버그
`onBillingTeamChange()` 의 `const keep = preferProject || ps.value` 가 **"비워라" 를
"지금 값 유지" 로 읽었다.** 빈 문자열이 falsy 라 `ps.value` 로 넘어가 버려서, 팀은 비었는데
프로젝트만 직전 선택이 남는 반쪽 상태가 됐다(실제로 화면에서 잡았다). `force` 인자를 두어
"창을 새로 채울 때"(빈 값이면 빈 채로)와 "사용자가 팀을 바꿀 때"(고르던 프로젝트 유지)를
갈랐다. **falsy 를 '값 없음' 으로 쓰는 기본값 관용구는 빈 문자열이 의미를 갖는 자리에서
이렇게 조용히 틀린다.**

### 곁다리로 정리한 것
`~/.nanobanana/prefs.json` 의 `last_billing_*` 에 `1팀 / TA Test` 가 들어가 있었다.
v1602 의 미리 고르기가 첫 항목을 골라둔 상태에서 확정된 값이라, 사용자가 의도한 선택이
아니었다. 비웠다.

검증: 스모크 24 / 귀속 18 / 탭격리 9 / 기존회귀 8 통과. 빈 칸·버튼 잠금·"이대로 채우기"·
팀만 골랐을 때 잠김 유지까지 실제 화면에서 확인. 자동 업데이트 E2E(1602→1603) PASS(12초).
